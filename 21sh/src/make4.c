#include "21sh.h"

t_red	*make_leftaddr(t_red *r, t_env *e)
{
	return (r);
}

t_red	*make_trileft_redirection(t_red *r, t_env *e)
{
	return (r);
}

t_red	*make_doubleleftless(t_red *r, t_env *e)
{
	return (r);
}

t_red	*make_doubleleft(t_red *r, t_env *e)
{
	return (r);
}

t_red	*make_leftpipe(t_red *r, t_env *e)
{
	return (r);
}
